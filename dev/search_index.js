var documenterSearchIndex = {"docs":
[{"category":"section","location":"logging/#Logging","page":"Logging","text":"","title":"Logging"},{"category":"section","location":"logging/#Functions","page":"Logging","text":"info: Info\nThese functions take a logger explicitly, so they do not need to discover logging state from the current task or consult the module registry. This makes them the preferred interface for hot paths, library internals, and execution contexts that need their own logger.","title":"Functions"},{"category":"section","location":"logging/#Explicit-logger-passing","page":"Logging","text":"clog(logger, group, level, msg...; kwargs...)\nclogenabled(logger, group, level)::Bool\n@clog logger group level msg...\n\nfunction solve(problem, logger)\n    clog(logger, :solver, 0, \"starting\")\n\n    clogenabled(logger, (:solver, :diagnostics)) && collect_diagnostics!(problem)\n\n    @clog logger (:solver, :summary) 1 \"objective = $(objective_value(problem))\"\nend","title":"Explicit logger passing"},{"category":"section","location":"logging/#Why-pass-a-logger-explicitly?","page":"Logging","text":"The standard-library logging macros (@info, @logmsg, ...) first look up the current task's logger, with the global logger as fallback. When a logger is already available—for example, in a const binding or a Ref—calling clog(logger, ...) bypasses that lookup, keeps the logging policy explicit, and helps keep hot paths very fast.","title":"Why pass a logger explicitly?"},{"category":"section","location":"logging/#Task-specific-loggers","page":"Logging","text":"The module-scoped model does not prevent task- or instance-specific logging. Use separate logger instances and pass them explicitly:\n\nlogger_a = ComponentLogger(...)\nlogger_b = ComponentLogger(...)\n\nThreads.@spawn solve(problem_a, logger_a)\nThreads.@spawn solve(problem_b, logger_b)\n\nThe two tasks now have independent component rules and sinks without requiring task-local logging state.","title":"Task-specific loggers"},{"category":"section","location":"logging/#Avoiding-unnecessary-work","page":"Logging","text":"clogenabled is intended to guard arbitrary work that should only run when a group/level is enabled:\n\nif clogenabled(logger, (:solver, :trace), -1000)\n    trace = compute_expensive_trace()\n    clog(logger, (:solver, :trace), -1000, \"trace\"; trace)\nend\n\nThe no-level form checks at Info:\n\nclogenabled(logger, group)\n\nThis also makes clogenabled useful as a lightweight runtime switch; see Hierarchical Runtime Control.\n\n@clog provides lazy message construction and automatically captures caller metadata:\n\n@clog logger :summary 0 begin\n    stats = compute_expensive_stats()\n    \"stats = $stats\"\nend","title":"Avoiding unnecessary work"},{"category":"section","location":"logging/#Forwarding-macro","page":"Logging","text":"At module top level, @forward_logger logger_expr creates local forwarding methods for clog, clogenabled, set_log_level!, get_log_level, and with_min_level, plus a shorter local @clog form bound to logger_expr:\n\nconst logger = ComponentLogger(...)\n@forward_logger logger\n\nclog(:core, 0, \"hello\")\n@clog :core 0 \"hello\"\n\nclogenabled(:core)\nset_log_level!(:core, true)\nget_log_level(:core)\n\nThe expression can be a Ref or field expression and is resolved at each call. Its global names are bound in the module that invokes @forward_logger, even if its generated @clog is imported elsewhere.","title":"Forwarding macro"},{"category":"section","location":"logging/#@clog-and-level-shorthands","page":"Logging","text":"@clog provides lazy logging with automatic caller metadata. Its level shorthands take an explicit logger argument at each call site.","title":"@clog and level shorthands"},{"category":"section","location":"logging/#Logging-macros","page":"Logging","text":"The main macro interface mirrors familiar logging operations while adding explicit component groups:\n\n@clog logger :solver 0 \"starting\"\n@cdebug logger :solver \"debug message\"\n@cinfo logger :solver \"information\"\n@cwarn logger :solver \"warning\"\n@cerror logger :solver \"error\"\n\nHierarchical tuple groups are also supported where accepted by the macro:\n\n@clog logger (:solver, :iteration) 0 \"iteration started\"","title":"Logging macros"},{"category":"section","location":"logging/#Why-use-@clog?","page":"Logging","text":"@clog evaluates message expressions only after logging is enabled and keeps them inline, avoiding closure capture of surrounding local values.\n\nIt also automatically captures the emitting call's module, file, and line. The function APIs require that metadata to be supplied manually when it is needed.\n\nWhen logging is enabled, @clog evaluates all message expressions from left to right. If the final expression evaluates to nothing, the log record is discarded and Logging.handle_message is not called. The @cdebug, @cinfo, @cwarn, and @cerror shorthands have the same behavior.","title":"Why use @clog?"},{"category":"section","location":"logging/#Caller-metadata","page":"Logging","text":"Macros automatically capture the caller's module, file, and line number. This is the main ergonomic advantage over the explicit function API when source-location metadata matters.","title":"Caller metadata"},{"category":"section","location":"logging/#Performance-trade-off","page":"Logging","text":"With an explicit logger, @clog uses it directly; a forwarded local @clog evaluates its bound logger expression directly. Neither uses a module registry lookup.\n\nThis keeps the macro path suitable for hot code while retaining lazy message evaluation and caller metadata.","title":"Performance trade-off"},{"category":"section","location":"logging/#Reference","page":"Logging","text":"","title":"Reference"},{"category":"function","location":"logging/#ComponentLogging.clog","page":"Logging","text":"clog(logger, group, level, msg...; _module, id, file, line, kwargs...)\n\nEmit a log message through the given logger. group is a Symbol or NTuple{N,Symbol}. level may be LogLevel or Integer. msg can be one or more values; tuples are passed through as-is.\n\nKeyword arguments file, line, and arbitrary kwargs... are forwarded to the underlying logger sink.\n\nIf @forward_logger is already used, the following forwarding signatures are available:\n\nclog(group, level, msg...; kwargs...)\nclog(group, msg...; kwargs...)\n\n\n\n\n\n","title":"ComponentLogging.clog"},{"category":"function","location":"logging/#ComponentLogging.clogenabled","page":"Logging","text":"clogenabled(logger, group, level) -> Bool\nclogenabled(logger, group) -> Bool\n\nReturn whether logging is enabled for the given logger, group, and level. If level is omitted, 0 is used.\n\nIf @forward_logger is already used, the following forwarding signatures are available:\n\nclogenabled(group, level) -> Bool\nclogenabled(group) -> Bool\n\n\n\n\n\n","title":"ComponentLogging.clogenabled"},{"category":"macro","location":"logging/#ComponentLogging.@forward_logger","page":"Logging","text":"@forward_logger logger_expr\n\nDefine forwarding methods in the current module so you can call clog, clogenabled, set_log_level!, get_log_level, and with_min_level without explicitly passing a logger each time. Also define a local @clog group level msg... bound to logger_expr.\n\nlogger_expr may be an AbstractLogger, a Ref holding one, or an expression such as STATE[].logger. Its names are bound in the module that invokes @forward_logger; importing the generated @clog elsewhere does not change which logger expression it uses. The expression is evaluated at each call, so replacing a Ref value affects subsequent calls.\n\nExample:\n\nusing ComponentLogging\n\nconst pkg_logger = Ref(ComponentLogger(...))\n@forward_logger pkg_logger\n\nclog(:core, 0, \"hello\")\n@clog :core 0 \"hello\"\nset_log_level!(:core, 1000)\nget_log_level(:core)\nwith_min_level(2000) do\n    # Temporarily raise this logger's minimum level (fast early rejection).\n    clog(:core, 0, \"suppressed by the temporary minimum\")\nend\n\nNote: Use this macro at module top-level.\n\n\n\n\n\n","title":"ComponentLogging.@forward_logger"},{"category":"macro","location":"logging/#ComponentLogging.@clog","page":"Logging","text":"@clog logger group level msg...\n\nMacro version of clog that captures the caller's Module, file, and line for accurate provenance. All positional arguments are required, including at least one message expression. group and level may be runtime expressions.\n\nMessage expressions are evaluated only after logging is enabled and remain inline, avoiding closure capture of surrounding local variables. Unlike clog, caller metadata is captured automatically rather than supplied as keywords.\n\nWhen logging is enabled, all message expressions are evaluated from left to right. If the final expression evaluates to nothing, the log record is discarded and Logging.handle_message is not called.\n\nExample:\n\n@clog logger :core 0 \"hello\"\n@clog logger (:a, :b) 2000 \"hello\"\n\n\n\n\n\n","title":"ComponentLogging.@clog"},{"category":"macro","location":"logging/#ComponentLogging.@cdebug","page":"Logging","text":"@cdebug logger group msg...\n\nShorthand for @clog logger group -2000 msg....\n\n\n\n\n\n","title":"ComponentLogging.@cdebug"},{"category":"macro","location":"logging/#ComponentLogging.@cinfo","page":"Logging","text":"@cinfo logger group msg...\n\nShorthand for @clog logger group 0 msg....\n\n\n\n\n\n","title":"ComponentLogging.@cinfo"},{"category":"macro","location":"logging/#ComponentLogging.@cwarn","page":"Logging","text":"@cwarn logger group msg...\n\nShorthand for @clog logger group 1000 msg....\n\n\n\n\n\n","title":"ComponentLogging.@cwarn"},{"category":"macro","location":"logging/#ComponentLogging.@cerror","page":"Logging","text":"@cerror logger group msg...\n\nShorthand for @clog logger group 2000 msg....\n\n\n\n\n\n","title":"ComponentLogging.@cerror"},{"category":"section","location":"runtime_control/#Hierarchical-Runtime-Control","page":"Hierarchical Runtime Control","text":"The group hierarchy in ComponentLogging is useful for more than deciding which messages are printed. Because clogenabled is a cheap Boolean query and log levels can be changed at runtime, the same hierarchy can serve as a lightweight runtime control plane for a module or component.\n\nInstead of threading many flags through a deep call stack,\n\nsolve(problem; presolve=true, heuristics=false, cache=true)\n\nand then forwarding those choices through every intermediate function, a component can place the behavior behind hierarchical groups and control it from the logger configuration.","title":"Hierarchical Runtime Control"},{"category":"section","location":"runtime_control/#Boolean-switches","page":"Hierarchical Runtime Control","text":"set_log_level! has a Boolean convenience method:\n\nset_log_level!(logger, group, true)\nset_log_level!(logger, group, false)\n\nIt also accepts multiple group, level pairs in one call:\n\nset_log_level!(logger,\n    (:solver, :presolve), true,\n    (:solver, :heuristics), false,\n    (:solver, :diagnostics), -1000,\n)\n\nInternally, true sets the group threshold to 0 (Info) and false sets it to 1. Since the no-level form of clogenabled checks at Info, this gives a compact on/off interface:\n\nclogenabled(logger, group)  # equivalent to checking at Info\n\nWith a logger forwarded at module scope,\n\nconst logger = ComponentLogger()\n@forward_logger logger\n\nthe same pattern becomes:\n\nset_log_level!(group, true)\nset_log_level!(group, false)\nclogenabled(group)","title":"Boolean switches"},{"category":"section","location":"runtime_control/#Example:-controlling-solver-behavior","page":"Hierarchical Runtime Control","text":"using ComponentLogging\n\nconst logger = ComponentLogger()\n@forward_logger logger\n\nfunction solve(problem)\n    clogenabled((:solver, :presolve)) && run_presolve!(problem)\n\n    if clogenabled((:solver, :heuristics))\n        run_heuristics!(problem)\n    end\n\n    clogenabled((:solver, :cache)) && update_cache!(problem)\n    return optimize!(problem)\nend\n\nset_log_level!((:solver, :presolve), true)\nset_log_level!((:solver, :heuristics), false)\nset_log_level!((:solver, :cache), true)\n\nsolve(problem)\n\nThe switch state belongs to the shared ComponentLogger, so behavior deep inside the component can be changed without modifying function signatures or passing configuration arguments through the call chain.","title":"Example: controlling solver behavior"},{"category":"section","location":"runtime_control/#Hierarchical-switches","page":"Hierarchical Runtime Control","text":"Groups are hierarchical. A rule for a more specific path takes precedence over its parent; otherwise the nearest matching parent is inherited, with :__default__ as the final fallback.\n\nFor example:\n\nrules = Dict(\n    :__default__ => 1,\n    :solver => 0,\n    (:solver, :heuristics) => 1,\n)\n\nlogger = ComponentLogger(rules)\n\nHere :solver is enabled at the default Info check, descendants of :solver inherit that setting unless they have a more specific rule, and (:solver, :heuristics) is explicitly disabled.\n\nThis makes it possible to switch an entire subsystem at once while keeping local overrides:\n\n:solver                    on\n├─ :presolve               inherits on\n├─ :heuristics             off\n└─ :linear_system          inherits on","title":"Hierarchical switches"},{"category":"section","location":"runtime_control/#More-than-Boolean-state","page":"Hierarchical Runtime Control","text":"The hierarchy is not limited to on/off switches. Because clogenabled also accepts an explicit level, log levels can act as multiple runtime thresholds.\n\nif clogenabled(logger, (:solver, :diagnostics), -1000)\n    collect_full_trace!()\nelseif clogenabled(logger, (:solver, :diagnostics), 0)\n    collect_summary!()\nend\n\nThis allows one hierarchy to encode progressively stronger modes without introducing a separate configuration system for each feature.\n\nStandard level values are:\n\nLevel Integer\nDebug -1000\nInfo 0\nWarn 1000\nError 2000\n\nCustom integer levels are also accepted through Julia's LogLevel representation.","title":"More than Boolean state"},{"category":"section","location":"runtime_control/#Explicit-per-instance-control","page":"Hierarchical Runtime Control","text":"Module-scoped state is the default design, but the same control-plane pattern can be made instance-specific by passing separate loggers explicitly:\n\nlogger_a = ComponentLogger()\nlogger_b = ComponentLogger()\n\nset_log_level!(logger_a, (:solver, :heuristics), true)\nset_log_level!(logger_b, (:solver, :heuristics), false)\n\nsolve(problem_a, logger_a)\nsolve(problem_b, logger_b)\n\nfunction solve(problem, logger)\n    clogenabled(logger, (:solver, :heuristics)) && run_heuristics!(problem)\n    return optimize!(problem)\nend\n\nThis gives separate tasks, solver instances, requests, or simulations independent runtime policies without introducing task-local logger lookup into the hot path.","title":"Explicit per-instance control"},{"category":"section","location":"runtime_control/#Concurrency","page":"Hierarchical Runtime Control","text":"Runtime switches use the same thread-safe configuration machinery as ordinary log-level updates. Since v0.3.0, copy-on-write snapshots with atomic publication keep normal reads lock-free while configuration changes remain safe across concurrent tasks and threads.\n\nThe result is a small hierarchical control mechanism that can sit directly in frequently executed code while still being changed dynamically from elsewhere in the application.","title":"Concurrency"},{"category":"section","location":"common_types/#Common-Types-and-Configuration","page":"Common Types","text":"","title":"Common Types and Configuration"},{"category":"section","location":"common_types/#ComponentLogger","page":"Common Types","text":"ComponentLogger is the central router/filter in ComponentLogging. It associates hierarchical group keys with minimum integer log levels and delegates accepted messages to an AbstractLogger sink.\n\nGroups can be a Symbol or a tuple of symbols:\n\n:solver\n(:solver, :iteration)\n(:solver, :linear_system)\n\nA more specific rule takes precedence over its parent. If no exact rule exists, lookup falls back through parent prefixes and finally to :__default__ (which defaults to Info when using the general dictionary constructor).\n\nlogger = ComponentLogger(Dict(\n    :solver => 1000,\n    (:solver, :iteration) => -2000,\n); sink=PlainLogger())\n\nHere :solver and unmatched descendants require level 1000, while (:solver, :iteration) accepts level -2000 and above.\n\ndisplay(logger) prints the configured hierarchy as a tree.","title":"ComponentLogger"},{"category":"section","location":"common_types/#Concurrency-and-ownership","page":"Common Types","text":"ComponentLogger is safe to share across tasks and threads. Rule updates are serialized and published as atomic copy-on-write snapshots, while normal reads remain lock-free. Each snapshot includes a cached minimum level for fast rejection.\n\ninfo: Info\nThread safety was introduced in v0.2.0. High-performance copy-on-write snapshots were introduced in v0.3.0.\n\nThe logger owns routing/filtering state, not final output. Accepted records are delegated to logger.sink; output thread safety therefore depends on the sink.","title":"Concurrency and ownership"},{"category":"section","location":"common_types/#Changing-rules","page":"Common Types","text":"Use set_log_level! to update rules:\n\nset_log_level!(logger, :solver, 1000)\nset_log_level!(logger, (:solver, :iteration), -2000)\n\nMultiple group, level pairs are applied atomically in one update:\n\nset_log_level!(logger,\n    :solver, 1000,\n    (:solver, :iteration), -2000,\n    (:solver, :heuristics), false,\n)\n\nBoolean levels provide a compact switch interface. true maps to 0; false maps to 1, which pairs naturally with the no-level clogenabled(logger, group) check. See Hierarchical Runtime Control for broader use of this mechanism.\n\nset_log_level!(logger, (:solver, :heuristics), true)\nset_log_level!(logger, (:solver, :heuristics), false)","title":"Changing rules"},{"category":"section","location":"common_types/#Inspecting-effective-levels","page":"Common Types","text":"Use get_log_level to query a group's effective level after hierarchical lookup.\n\nlogger = ComponentLogger(Dict(:solver => 1000))\nget_log_level(logger, (:solver, :iteration))\n# Warn","title":"Inspecting effective levels"},{"category":"section","location":"common_types/#Temporary-global-minimum-level","page":"Common Types","text":"with_min_level temporarily changes the minimum level of one ComponentLogger for the duration of a callback:\n\nwith_min_level(logger, 2000) do\n    run_workload()\nend\n\nThis is a logger-wide temporary override, not a task-local equivalent of Logging.with_logger. All users of the target logger observe the temporary minimum until the callback exits, after which the previous state is restored even if the callback throws.\n\nwarning: Thread safety\nThe temporary level applies to every task and thread using that logger. The old snapshot is restored even if the callback throws, so configuration changes to the same logger inside the callback do not persist afterward.","title":"Temporary global minimum level"},{"category":"section","location":"common_types/#PlainLogger","page":"Common Types","text":"PlainLogger is an independent AbstractLogger sink that keeps console output close to ordinary print/println output instead of adding the standard [ Info:-style presentation. It can be used as the sink of a ComponentLogger or on its own with Julia's standard with_logger.\n\nsink = PlainLogger()\nlogger = ComponentLogger(Dict(:core => 0); sink)\nclog(logger, :core, 0, \"hello\")\n\nRouting and presentation are intentionally separate: ComponentLogger applies its component rules, while PlainLogger (or any other AbstractLogger sink) applies its own filtering and decides how accepted records are written.","title":"PlainLogger"},{"category":"section","location":"common_types/#Reference","page":"Common Types","text":"","title":"Reference"},{"category":"module","location":"common_types/#ComponentLogging.ComponentLogging","page":"Common Types","text":"ComponentLogging\n\nHierarchical component logging built on Julia's standard Logging interface. This package provides:\n\nA ComponentLogger with hierarchical rule keys to control log levels per component path, e.g. (:net, :http).\nExplicit functions clog and clogenabled.\nExplicit logging macros @clog, @cdebug, @cinfo, @cwarn, and @cerror with caller metadata.\n@forward_logger, which creates module-local forwarding functions and a local @clog.\nA simple PlainLogger sink for plain output without timestamps or standard prefixes.\n\nTypical usage:\n\nusing ComponentLogging\n\nrules = Dict(\n    :core => 0,\n    :io => 1000,\n    :net => -2000,\n)\nclogger = ComponentLogger(rules; sink=PlainLogger())\n\nclog(clogger, :core, 0, \"something happened\")\n\n\n\n\n\n","title":"ComponentLogging.ComponentLogging"},{"category":"type","location":"common_types/#ComponentLogging.ComponentLogger","page":"Common Types","text":"ComponentLogger(; sink=ConsoleLogger(Debug))\nComponentLogger(rules::AbstractDict; sink=ConsoleLogger(Debug))\n\nA logger that delegates to an underlying sink (AbstractLogger) while applying component-based minimum level rules. Rules are defined on paths of symbols (NTuple{N,Symbol}). A lookup walks up the path and falls back to (:__default__,).\n\nrules: mapping from NTuple{N,Symbol} to LogLevel. If no explicit (:__default__,) rule exists, lookup falls back to Info.\nsink: the underlying AbstractLogger that actually handles messages.\nConfiguration updates are serialized and published as atomic copy-on-write (COW) snapshots.\nEach snapshot includes a min_level cache for fast checks.\n\n\n\n\n\n","title":"ComponentLogging.ComponentLogger"},{"category":"type","location":"common_types/#ComponentLogging.PlainLogger","page":"Common Types","text":"PlainLogger(; stream=Base.CoreLogging.closed_stream, min_level=Info)\n\nA simple AbstractLogger implementation that prints messages without standard prefixes or timestamps.\n\nstream::IO: target stream; if closed, falls back to stderr.\nmin_level::LogLevel: minimum enabled level for the sink.\n\nIntended for tests, demos, or embedding in custom sinks.\n\nWhen used as a ComponentLogger sink, a record must pass both the effective component level and PlainLogger.min_level.\n\n\n\n\n\n","title":"ComponentLogging.PlainLogger"},{"category":"function","location":"common_types/#ComponentLogging.set_log_level!","page":"Common Types","text":"set_log_level!(logger, group, lvl, args...) -> ComponentLogger\n\nSet or update the minimum level for a specific component group on logger. group may be a Symbol or a NTuple{N,Symbol} tuple; lvl can be LogLevel, Integer, or Bool. Additional args... are accepted as more group, level pairs and are applied atomically in one update.\n\nIf a level is a Bool, it is treated as a simple switch: true sets the rule to 0 and false sets it to 1 (which disables the default clogenabled(logger, group) check).\n\nRule updates are thread-safe and atomic.\n\nExample:\n\nlogger = ComponentLogger()\nset_log_level!(logger, :solver, 1000, (:solver, :iteration), -1000)\n\n\n\n\n\n","title":"ComponentLogging.set_log_level!"},{"category":"function","location":"common_types/#ComponentLogging.get_log_level","page":"Common Types","text":"get_log_level(logger, group) -> LogLevel\n\nReturn the effective minimum log level for group on logger. group may be a Symbol or an NTuple{N,Symbol} component path. Lookup checks the exact path, then its parent paths, and finally (:__default__,).\n\nExample:\n\nlogger = ComponentLogger(Dict(:solver => 1000))\nget_log_level(logger, (:solver, :iteration))\n# Warn\n\n\n\n\n\n","title":"ComponentLogging.get_log_level"},{"category":"function","location":"common_types/#ComponentLogging.with_min_level","page":"Common Types","text":"with_min_level(f, logger, lvl)\n\nTemporarily override the minimum enabled level for logger while executing f(). The override applies to every task and thread that uses this logger, and the previous state is restored when f() returns or throws. Configuration changes made to the logger during the callback are discarded when that state is restored.\n\n\n\n\n\n","title":"ComponentLogging.with_min_level"},{"category":"section","location":"#ComponentLogging","page":"Home","text":"ComponentLogging.jl is a lightweight, high-performance logging layer for Julia built around explicit component loggers and hierarchical log-level control. A ComponentLogger applies hierarchical rules to groups such as :solver or (:solver, :iteration) and delegates accepted records to any AbstractLogger sink.\n\nThe package is designed for software where logging policy naturally belongs to a module or component and where filtered logging may sit on extremely hot paths.","title":"ComponentLogging"},{"category":"section","location":"#Logger-ownership:-explicit-scope-vs-task-scope","page":"Home","text":"Julia's standard Logging system dynamically selects the active logger from the current task, with a global logger as fallback. This makes the logger part of the current execution context: different tasks can carry different loggers, and application code can control logging for an entire dynamic call tree with with_logger.\n\nComponentLogging deliberately chooses a different default ownership model. A ComponentLogger is supplied explicitly, either passed through calls or bound by @forward_logger to module-local forwarding APIs, rather than selected from task-local state.\n\nFor example, a solver module can define one hierarchy:\n\n:solver\n(:solver, :iteration)\n(:solver, :linear_system)\n\nand all code using that module-level logger observes the same component configuration, independent of which task or thread happens to execute the code.\n\nThis is a deliberate trade-off, not a replacement for Julia's logging model. Julia's own documentation explicitly contrasts its dynamically scoped logger design with frameworks where loggers are lexically scoped or explicitly provided by module authors. ComponentLogging intentionally occupies that other point in the design space.","title":"Logger ownership: explicit scope vs task scope"},{"category":"section","location":"#Design-philosophy","page":"Home","text":"ComponentLogging is designed around one central constraint:\n\nFiltered logging should be as close to free as possible.\n\nIn performance-sensitive numerical and systems code, disabled log calls may execute millions of times. When the useful work of a filtered call is only a few nanoseconds, a task-local lookup, reader-side lock, allocation, or other per-call synchronization can easily dominate the cost.\n\nComponentLogging therefore keeps the normal read path deliberately small:\n\nmodule/component configuration is shared rather than discovered from task-local state on every call;\nthe explicit clog and clogenabled APIs bypass task-local logger lookup entirely;\nnormal ComponentLogger reads do not acquire a configuration lock;\ncomparatively rare configuration changes pay the synchronization cost instead.\n\nThis makes the explicit function API particularly suitable for hot loops and performance-sensitive library internals. It also keeps logging behavior explicit when a logger is passed through a call chain.","title":"Design philosophy"},{"category":"section","location":"#Concurrency-model","page":"Home","text":"ComponentLogger is designed for safe concurrent use across tasks and threads. Thread-safe configuration updates were introduced in v0.2.0, and since v0.3.0 the implementation has used copy-on-write snapshots with atomic publication, keeping normal logging reads lock-free while preserving safe concurrent configuration updates.\n\nwith_min_level temporarily changes the minimum level globally for the target ComponentLogger, so all tasks and threads using that logger observe the temporary setting until it is restored.\n\nComponentLogger only routes and filters messages. Accepted records are delegated to the configured AbstractLogger sink, so thread safety of the actual message output is the responsibility of that sink.","title":"Concurrency model"},{"category":"section","location":"#Two-complementary-usage-modes","page":"Home","text":"Module-local convenience. Use @forward_logger to create forwarding APIs when one shared logging policy should apply throughout that component.\n\nExplicit logger passing. When a particular task, request, solver instance, or other execution context needs its own logging policy, create a separate ComponentLogger and pass it explicitly:\n\nlogger_a = ComponentLogger(...)\nlogger_b = ComponentLogger(...)\n\nThreads.@spawn solve(problem_a, logger_a)\nThreads.@spawn solve(problem_b, logger_b)\n\nfunction solve(problem, logger)\n    clog(logger, :iteration, 0, \"starting iteration\")\nend\n\nThis provides task-specific logging without making every log call query task-local state and remains fast in hot paths.","title":"Two complementary usage modes"},{"category":"section","location":"#Beyond-logging","page":"Home","text":"The same hierarchy can also act as a lightweight runtime control plane. clogenabled can guard arbitrary work, while Boolean set_log_level! calls turn hierarchical groups into runtime switches without threading configuration flags through every function in the call stack.\n\nThis can be used for diagnostics, tracing, optional computations, algorithmic branches, caching strategies, instrumentation, or any other behavior placed behind the hierarchy.\n\nSee Hierarchical Runtime Control for the full pattern and examples.","title":"Beyond logging"},{"category":"section","location":"#Performance-tracking","page":"Home","text":"ComponentLogging is continuously benchmarked over time. Current and historical performance results are available on the benchmark dashboard.","title":"Performance tracking"},{"category":"section","location":"#API-index","page":"Home","text":"Modules = [ComponentLogging]\nOrder   = [:type, :function, :macro]","title":"API index"}]
}
