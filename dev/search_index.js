var documenterSearchIndex = {"docs":
[{"category":"section","location":"functions/#Function-API","page":"Functions","text":"The function API is the performance-oriented and context-explicit interface to ComponentLogging.\n\nThese functions take a logger explicitly, so they do not need to discover logging state from the current task or consult the module registry. This makes them the preferred interface for hot paths, library internals, and execution contexts that need their own logger.","title":"Function API"},{"category":"section","location":"functions/#Explicit-logger-passing","page":"Functions","text":"clog(logger, group, level, msg...; kwargs...)\nclogenabled(logger, group, level)::Bool\nclogf(f::Function, logger, group, level)\n\nA logger can be passed through an ordinary call chain:\n\nfunction solve(problem, logger)\n    clog(logger, :solver, 0, \"starting\")\n\n    clogenabled(logger, (:solver, :diagnostics)) && collect_diagnostics!(problem)\n\n    clogf(logger, (:solver, :summary), 0) do\n        \"objective = $(objective_value(problem))\"\n    end\nend\n\nBecause the concrete ComponentLogger{L} type can remain visible throughout the call chain, Julia can specialize on the sink type as well as bypassing both task-local lookup and module-registry lookup.","title":"Explicit logger passing"},{"category":"section","location":"functions/#Task-specific-loggers","page":"Functions","text":"The module-scoped model does not prevent task- or instance-specific logging. Use separate logger instances and pass them explicitly:\n\nlogger_a = ComponentLogger(...)\nlogger_b = ComponentLogger(...)\n\nThreads.@spawn solve(problem_a, logger_a)\nThreads.@spawn solve(problem_b, logger_b)\n\nThe two tasks now have independent component rules and sinks without requiring task-local logging state.","title":"Task-specific loggers"},{"category":"section","location":"functions/#Avoiding-unnecessary-work","page":"Functions","text":"clogenabled is intended to guard arbitrary work that should only run when a group/level is enabled:\n\nif clogenabled(logger, (:solver, :trace), -1000)\n    trace = compute_expensive_trace()\n    clog(logger, (:solver, :trace), -1000, \"trace\"; trace)\nend\n\nThe no-level form checks at Info:\n\nclogenabled(logger, group)\n\nThis also makes clogenabled useful as a lightweight runtime switch; see Hierarchical Runtime Control.\n\nclogf provides lazy message construction. Its callback is evaluated only when the requested group/level is enabled:\n\nclogf(logger, :summary, 0) do\n    stats = compute_expensive_stats()\n    \"stats = $stats\"\nend","title":"Avoiding unnecessary work"},{"category":"section","location":"functions/#Forwarding-macro","page":"Functions","text":"@forward_logger generates module-local forwarding methods for clog, clogenabled, clogf, set_log_level, and with_min_level, allowing one known logger to be used without writing it at every call site.\n\nconst logger = ComponentLogger(...)\n@forward_logger logger\n\nclog(:core, 0, \"hello\")\nclogenabled(:core)\nset_log_level(:core, true)\n\nThe forwarded logger expression may also be a Ref, which is useful when the logger object itself needs to be replaced while keeping the forwarding methods stable.\n\ninfo: Info\nThe function APIs do not automatically capture the caller's module, file, or line information. Supply them explicitly when that metadata is needed:clog(logger, :core, 0, \"hello\"; _module=@__MODULE__, file=@__FILE__, line=@__LINE__)\n\nFor automatic caller metadata and module-bound lookup, use the Macros API.","title":"Forwarding macro"},{"category":"section","location":"functions/#Reference","page":"Functions","text":"","title":"Reference"},{"category":"function","location":"functions/#ComponentLogging.clog","page":"Functions","text":"clog(logger, group, level, msg...; _module, file, line, kwargs...)\n\nEmit a log message through the given or implicit logger. group is a Symbol or NTuple{N,Symbol}. level may be LogLevel or Integer. msg can be one or more values; tuples are passed through as-is.\n\nKeyword arguments file, line, and arbitrary kwargs... are forwarded to the underlying logger sink.\n\nIf @forward_logger is already used, the following forwarding signatures are available:\n\nclog(group, level, msg...; kwargs...)\nclog(group, msg...; kwargs...)\n\n\n\n\n\n","title":"ComponentLogging.clog"},{"category":"function","location":"functions/#ComponentLogging.clogenabled","page":"Functions","text":"clogenabled(logger, group, level) -> Bool\nclogenabled(logger, group) -> Bool\n\nReturn whether logging is enabled for the given logger, group, and level. If level is omitted, Info is used.\n\nIf @forward_logger is already used, the following forwarding signatures are available:\n\nclogenabled(group, level) -> Bool\nclogenabled(group) -> Bool\n\n\n\n\n\n","title":"ComponentLogging.clogenabled"},{"category":"function","location":"functions/#ComponentLogging.clogf","page":"Functions","text":"clogf(f::Function, logger, group, level; _module, file, line)\n\nLike clog, but accepts a zero-argument function f that is only invoked if logging is enabled for the specified group and level. If f() returns nothing, no message is emitted. Non-tuple returns are converted to a tuple internally.\n\nIf @forward_logger is already used, the following forwarding signatures are available:\n\nclogf(f, group, level; _module, file, line)\n\n\n\n\n\n","title":"ComponentLogging.clogf"},{"category":"macro","location":"functions/#ComponentLogging.@forward_logger","page":"Functions","text":"@forward_logger logger\n\nDefine forwarding methods in the current module so you can call clog, clogf, clogenabled, set_log_level, and with_min_level without explicitly passing a logger each time.\n\nlogger may be either an AbstractLogger or a Base.RefValue{<:AbstractLogger}.\n\nExample:\n\nusing ComponentLogging\n\nconst pkg_logger = Ref(ComponentLogger(...))\n@forward_logger pkg_logger\n\nclog(:core, 0, \"hello\")\nclogf(:core, 0) do\n    (\"expensive \", 1 + 2)\nend\nset_log_level(:core, 1000)\nwith_min_level(2000) do\n    # Temporarily raise the current task's minimum level (fast early rejection).\n    clog(:core, 0, \"suppressed by the task-local minimum\")\nend\n\nNote: Use this macro at module top-level.\n\n\n\n\n\n","title":"ComponentLogging.@forward_logger"},{"category":"section","location":"macros/#Macros-API","page":"Macros","text":"The macro API provides module-bound convenience. Unlike the explicit function API, logging macros do not take a logger argument at each call site. Instead, they resolve the logger associated with the calling module through ComponentLogging's module registry.","title":"Macros API"},{"category":"section","location":"macros/#Module-registry","page":"Macros","text":"A logger can be bound directly:\n\nset_module_logger(@__MODULE__, logger)\n\nor with @bind_logger:\n\n@bind_logger sink=logger\n\nFor packages, binding is commonly performed from __init__:\n\nfunction __init__()\n    set_module_logger(@__MODULE__, logger)\nend\n\nIf the calling module has no direct binding, get_logger walks through parent modules until it finds one. This allows a package-level binding to serve submodules by default while still permitting a submodule to install its own logger when needed.\n\nMyPackage.Submodule\n        │\n        ├─ direct binding? ── yes → use it\n        │\n        └─ no\n           ↓\n      MyPackage binding\n\nModule bindings are safe to update concurrently. Since v0.3.0 the registry uses the same copy-on-write snapshot model as ComponentLogger, so lookups remain lock-free on the normal read path.","title":"Module registry"},{"category":"section","location":"macros/#Logging-macros","page":"Macros","text":"The main macro interface mirrors familiar logging operations while adding explicit component groups:\n\n@clog :solver 0 \"starting\"\n@cdebug :solver \"debug message\"\n@cinfo :solver \"information\"\n@cwarn :solver \"warning\"\n@cerror :solver \"error\"\n\nHierarchical tuple groups are also supported where accepted by the macro:\n\n@clog (:solver, :iteration) 0 \"iteration started\"\n\n@clogenabled and @clogf provide the macro equivalents of the corresponding function APIs.","title":"Logging macros"},{"category":"section","location":"macros/#Caller-metadata","page":"Macros","text":"Macros automatically capture the caller's module, file, and line number. This is the main ergonomic advantage over the explicit function API when source-location metadata matters.","title":"Caller metadata"},{"category":"section","location":"macros/#Performance-trade-off","page":"Macros","text":"The macro path performs a module-registry lookup before reaching the logger. This is intentionally a convenience path rather than the minimum-overhead path.\n\nFor extremely hot code, or when a logger already exists as an explicit argument, prefer clog, clogenabled, and clogf directly. See Function API.","title":"Performance trade-off"},{"category":"section","location":"macros/#Reference","page":"Macros","text":"","title":"Reference"},{"category":"function","location":"macros/#ComponentLogging.set_module_logger","page":"Macros","text":"set_module_logger(mod::Module, logger::AbstractLogger) -> String\n\nBind logger to the module mod. Returns a short human-readable string summary \"<Module> <- <LoggerType>\".\n\n\n\n\n\n","title":"ComponentLogging.set_module_logger"},{"category":"function","location":"macros/#ComponentLogging.get_logger","page":"Macros","text":"get_logger(mod::Module) -> AbstractLogger\n\nReturn the logger bound to module mod, walking up parent modules if necessary. Throws an error if none is found at the root.\n\n\n\n\n\n","title":"ComponentLogging.get_logger"},{"category":"macro","location":"macros/#ComponentLogging.@bind_logger","page":"Macros","text":"@bind_logger [sink=...] [rules=...] [mod=...]\n\nBind a ComponentLogger to the given mod (default: caller's module). Arguments must be passed as keywords. rules may be any AbstractDict mapping groups (Symbol or NTuple{N,Symbol}) to levels (LogLevel or Integer).\n\nReturns the constructed ComponentLogger.\n\nExample:\n\n@bind_logger sink=ConsoleLogger() rules=Dict(:__default__=>Info, :core=>Warn)\n\n\n\n\n\n","title":"ComponentLogging.@bind_logger"},{"category":"macro","location":"macros/#ComponentLogging.@clog","page":"Macros","text":"@clog [group] level msg...\n\nMacro version of clog that captures the caller's Module, file, and line for accurate provenance. group must be a literal Symbol or tuple of literal symbols.\n\nExample:\n\n@clog 0 \"hello\"             # default group\n@clog :core 1000 \"hello\"    # single group (literal)\n@clog (:a,:b) 2000 \"hello\"  # specified group (literal)\n\n\n\n\n\n","title":"ComponentLogging.@clog"},{"category":"macro","location":"macros/#ComponentLogging.@cdebug","page":"Macros","text":"@cdebug args...\n\nShorthand for @clog Debug args.... Emits a message at Debug level. See @clog for argument rules and caller metadata capture.\n\n\n\n\n\n","title":"ComponentLogging.@cdebug"},{"category":"macro","location":"macros/#ComponentLogging.@cinfo","page":"Macros","text":"@cinfo args...\n\nShorthand for @clog Info args.... Emits a message at Info level. See @clog for argument rules and caller metadata capture.\n\n\n\n\n\n","title":"ComponentLogging.@cinfo"},{"category":"macro","location":"macros/#ComponentLogging.@cwarn","page":"Macros","text":"@cwarn args...\n\nShorthand for @clog Warn args.... Emits a message at Warn level. See @clog for argument rules and caller metadata capture.\n\n\n\n\n\n","title":"ComponentLogging.@cwarn"},{"category":"macro","location":"macros/#ComponentLogging.@cerror","page":"Macros","text":"@cerror args...\n\nShorthand for @clog Error args.... Emits a message at Error level. See @clog for argument rules and caller metadata capture.\n\n\n\n\n\n","title":"ComponentLogging.@cerror"},{"category":"macro","location":"macros/#ComponentLogging.@clogenabled","page":"Macros","text":"@clogenabled group level\n\nMacro that expands to a boolean expression answering whether logging is enabled for the literal group and level at the call site (using the logger bound to the caller's module). group must be a literal Symbol or tuple of literal symbols.\n\n\n\n\n\n","title":"ComponentLogging.@clogenabled"},{"category":"macro","location":"macros/#ComponentLogging.@clogf","page":"Macros","text":"@clogf [group] level expr\n\nMacro version of clogf. The last argument can be either a message expression or a zero-argument function (e.g. () -> begin ...; \"message\" end). The body is only evaluated if logging is enabled. Caller module and source location are captured automatically.\n\n\n\n\n\n","title":"ComponentLogging.@clogf"},{"category":"section","location":"runtime_control/#Hierarchical-Runtime-Control","page":"Hierarchical Runtime Control","text":"The group hierarchy in ComponentLogging is useful for more than deciding which messages are printed. Because clogenabled is a cheap Boolean query and log levels can be changed at runtime, the same hierarchy can serve as a lightweight runtime control plane for a module or component.\n\nInstead of threading many flags through a deep call stack,\n\nsolve(problem; presolve=true, heuristics=false, cache=true)\n\nand then forwarding those choices through every intermediate function, a component can place the behavior behind hierarchical groups and control it from the logger configuration.","title":"Hierarchical Runtime Control"},{"category":"section","location":"runtime_control/#Boolean-switches","page":"Hierarchical Runtime Control","text":"set_log_level! has a Boolean convenience method:\n\nset_log_level!(logger, group, true)\nset_log_level!(logger, group, false)\n\nInternally, true sets the group threshold to 0 (Info) and false sets it to 1. Since the no-level form of clogenabled checks at Info, this gives a compact on/off interface:\n\nclogenabled(logger, group)  # equivalent to checking at Info\n\nWith a logger forwarded at module scope,\n\nconst logger = ComponentLogger()\n@forward_logger logger\n\nthe same pattern becomes:\n\nset_log_level(group, true)\nset_log_level(group, false)\nclogenabled(group)","title":"Boolean switches"},{"category":"section","location":"runtime_control/#Example:-controlling-solver-behavior","page":"Hierarchical Runtime Control","text":"using ComponentLogging\n\nconst logger = ComponentLogger()\n@forward_logger logger\n\nfunction solve(problem)\n    clogenabled((:solver, :presolve)) && run_presolve!(problem)\n\n    if clogenabled((:solver, :heuristics))\n        run_heuristics!(problem)\n    end\n\n    clogenabled((:solver, :cache)) && update_cache!(problem)\n    return optimize!(problem)\nend\n\nset_log_level((:solver, :presolve), true)\nset_log_level((:solver, :heuristics), false)\nset_log_level((:solver, :cache), true)\n\nsolve(problem)\n\nThe switch state belongs to the shared ComponentLogger, so behavior deep inside the component can be changed without modifying function signatures or passing configuration arguments through the call chain.","title":"Example: controlling solver behavior"},{"category":"section","location":"runtime_control/#Hierarchical-switches","page":"Hierarchical Runtime Control","text":"Groups are hierarchical. A rule for a more specific path takes precedence over its parent; otherwise the nearest matching parent is inherited, with :__default__ as the final fallback.\n\nFor example:\n\nrules = Dict(\n    :__default__ => 1,\n    :solver => 0,\n    (:solver, :heuristics) => 1,\n)\n\nlogger = ComponentLogger(rules)\n\nHere :solver is enabled at the default Info check, descendants of :solver inherit that setting unless they have a more specific rule, and (:solver, :heuristics) is explicitly disabled.\n\nThis makes it possible to switch an entire subsystem at once while keeping local overrides:\n\n:solver                    on\n├─ :presolve               inherits on\n├─ :heuristics             off\n└─ :linear_system          inherits on","title":"Hierarchical switches"},{"category":"section","location":"runtime_control/#More-than-Boolean-state","page":"Hierarchical Runtime Control","text":"The hierarchy is not limited to on/off switches. Because clogenabled also accepts an explicit level, log levels can act as multiple runtime thresholds.\n\nif clogenabled(logger, (:solver, :diagnostics), -1000)\n    collect_full_trace!()\nelseif clogenabled(logger, (:solver, :diagnostics), 0)\n    collect_summary!()\nend\n\nThis allows one hierarchy to encode progressively stronger modes without introducing a separate configuration system for each feature.\n\nStandard level values are:\n\nLevel Integer\nDebug -1000\nInfo 0\nWarn 1000\nError 2000\n\nCustom integer levels are also accepted through Julia's LogLevel representation.","title":"More than Boolean state"},{"category":"section","location":"runtime_control/#Explicit-per-instance-control","page":"Hierarchical Runtime Control","text":"Module-scoped state is the default design, but the same control-plane pattern can be made instance-specific by passing separate loggers explicitly:\n\nlogger_a = ComponentLogger()\nlogger_b = ComponentLogger()\n\nset_log_level!(logger_a, (:solver, :heuristics), true)\nset_log_level!(logger_b, (:solver, :heuristics), false)\n\nsolve(problem_a, logger_a)\nsolve(problem_b, logger_b)\n\nfunction solve(problem, logger)\n    clogenabled(logger, (:solver, :heuristics)) && run_heuristics!(problem)\n    return optimize!(problem)\nend\n\nThis gives separate tasks, solver instances, requests, or simulations independent runtime policies without introducing task-local logger lookup into the hot path.","title":"Explicit per-instance control"},{"category":"section","location":"runtime_control/#Concurrency","page":"Hierarchical Runtime Control","text":"Runtime switches use the same thread-safe configuration machinery as ordinary log-level updates. Since v0.3.0, copy-on-write snapshots with atomic publication keep normal reads lock-free while configuration changes remain safe across concurrent tasks and threads.\n\nThe result is a small hierarchical control mechanism that can sit directly in frequently executed code while still being changed dynamically from elsewhere in the application.","title":"Concurrency"},{"category":"section","location":"common_types/#Common-Types-and-Configuration","page":"Common Types","text":"","title":"Common Types and Configuration"},{"category":"section","location":"common_types/#ComponentLogger","page":"Common Types","text":"ComponentLogger is the central router/filter in ComponentLogging. It associates hierarchical group keys with minimum log levels and delegates accepted messages to an AbstractLogger sink.\n\nGroups can be a Symbol or a tuple of symbols:\n\n:solver\n(:solver, :iteration)\n(:solver, :linear_system)\n\nA more specific rule takes precedence over its parent. If no exact rule exists, lookup falls back through parent prefixes and finally to :__default__ (which defaults to Info when using the general dictionary constructor).\n\nlogger = ComponentLogger(Dict(\n    :__default__ => 0,\n    :solver => 1000,\n    (:solver, :iteration) => -1000,\n); sink=PlainLogger())\n\nIn this example, :solver and its unmatched descendants require Warn, while (:solver, :iteration) explicitly accepts Debug and above.\n\ndisplay(logger) prints the configured hierarchy in tree form, which is useful when inspecting a larger component configuration.","title":"ComponentLogger"},{"category":"section","location":"common_types/#Concurrency-and-ownership","page":"Common Types","text":"ComponentLogger is designed for shared concurrent use. Thread-safe configuration updates were introduced in v0.2.0; since v0.3.0, copy-on-write snapshots with atomic publication keep normal reads lock-free while configuration updates remain safe.\n\nThe logger owns routing/filtering state, not the final output mechanism. Accepted messages are delegated to logger.sink, so message-output thread safety depends on the selected sink.","title":"Concurrency and ownership"},{"category":"section","location":"common_types/#Changing-rules","page":"Common Types","text":"Use set_log_level! to update one group dynamically:\n\nset_log_level!(logger, :solver, 1000)\nset_log_level!(logger, (:solver, :iteration), -1000)\n\nBoolean values provide a compact switch interface:\n\nset_log_level!(logger, (:solver, :heuristics), true)\nset_log_level!(logger, (:solver, :heuristics), false)\n\ntrue maps to level 0 (Info) and false maps to level 1, which pairs naturally with the no-level clogenabled(logger, group) check. See Hierarchical Runtime Control for broader use of this mechanism.","title":"Changing rules"},{"category":"section","location":"common_types/#Temporary-global-minimum-level","page":"Common Types","text":"with_min_level temporarily changes the minimum level of one ComponentLogger for the duration of a callback:\n\nwith_min_level(logger, 2000) do\n    # The temporary minimum applies to every task/thread using this logger.\n    run_workload()\nend\n\nThis is a logger-wide temporary override, not a task-local equivalent of Logging.with_logger. All users of the target logger observe the temporary minimum until the callback exits, after which the previous state is restored even if the callback throws.\n\nTreat the block as a temporary configuration scope: configuration changes made to the same logger inside the block do not persist after the outer snapshot is restored.","title":"Temporary global minimum level"},{"category":"section","location":"common_types/#PlainLogger","page":"Common Types","text":"PlainLogger is an independent AbstractLogger sink that keeps console output close to ordinary print/println output instead of adding the standard [ Info:-style presentation. It can be used as the sink of a ComponentLogger or on its own with Julia's standard with_logger.\n\nusing ComponentLogging, Logging\n\nsink = PlainLogger()\nlogger = ComponentLogger(Dict(:core => 0); sink)\n\nclog(logger, :core, 0, \"hello\")\n\nRouting and presentation are intentionally separate: ComponentLogger decides whether a record passes, while PlainLogger (or any other AbstractLogger sink) decides how accepted records are written.","title":"PlainLogger"},{"category":"section","location":"common_types/#Reference","page":"Common Types","text":"","title":"Reference"},{"category":"module","location":"common_types/#ComponentLogging.ComponentLogging","page":"Common Types","text":"ComponentLogging\n\nModule-scoped logging utilities for Julia built on top of the stdlib Logging. This package provides:\n\nA ComponentLogger with hierarchical rule keys to control log levels per component path, e.g. (:net, :http).\nLightweight functions clog, clogenabled, clogf for emitting messages and checking if logging is enabled.\nMacros @clog, @clogf, @clogenabled that capture the caller module/source location for accurate provenance.\nMacro @forward_logger to generate module-local forwarding methods.\nA simple PlainLogger sink for pretty, colored output without timestamps/prefixes.\n\nTypical usage:\n\nusing ComponentLogging\n\nrules = Dict(\n    :core => Info,\n    :io => Warn,\n    :net => Debug\n)\nclogger = ComponentLogger(rules; sink=PlainLogger())\n\nclog(clogger, :core, Info, \"something happened\")\n\n\n\n\n\n","title":"ComponentLogging.ComponentLogging"},{"category":"type","location":"common_types/#ComponentLogging.ComponentLogger","page":"Common Types","text":"ComponentLogger(; sink=ConsoleLogger(Debug))\nComponentLogger(rules::AbstractDict; sink=ConsoleLogger(Debug))\n\nA logger that delegates to an underlying sink (AbstractLogger) while applying component-based minimum level rules. Rules are defined on paths of symbols (NTuple{N,Symbol}). A lookup walks up the path and falls back to (:__default__,).\n\nrules: mapping from NTuple{N,Symbol} to LogLevel. If no explicit (:__default__,) rule exists, lookup falls back to Info.\nsink: the underlying AbstractLogger that actually handles messages.\nlock: protects concurrent access to rules.\nmin_level: atomic cache of the minimum value in rules for fast checks.\n\n\n\n\n\n","title":"ComponentLogging.ComponentLogger"},{"category":"type","location":"common_types/#ComponentLogging.PlainLogger","page":"Common Types","text":"PlainLogger(; stream=Base.CoreLogging.closed_stream, min_level=Info)\n\nA simple AbstractLogger implementation that prints messages without standard prefixes/timestamps, with minimal coloring by level.\n\nstream::IO: target stream; if closed, falls back to stderr.\nmin_level::LogLevel: minimum enabled level for the sink.\n\nIntended for tests, demos, or embedding in custom sinks.\n\n\n\n\n\n","title":"ComponentLogging.PlainLogger"},{"category":"function","location":"common_types/#ComponentLogging.set_log_level!","page":"Common Types","text":"set_log_level!(logger, group, lvl) -> ComponentLogger\n\nSet or update the minimum level for a specific component group on logger. group may be a Symbol or a NTuple{N,Symbol} tuple; lvl can be LogLevel or Integer. If lvl is a Bool, it is treated as a simple switch: true sets the rule to Info and false sets it to LogLevel(1) (which disables the default clogenabled(logger, group) check). Updates rules under logger.lock and keeps the atomic min_level cache consistent.\n\n\n\n\n\n","title":"ComponentLogging.set_log_level!"},{"category":"function","location":"common_types/#ComponentLogging.with_min_level","page":"Common Types","text":"with_min_level(f, logger, lvl)\n\nTemporarily override the minimum enabled level for the current task while executing f(). The override is task-local, so it does not modify logger.min_level or affect other concurrent tasks. Nested overrides are restored correctly, including when f() throws an exception.\n\n\n\n\n\n","title":"ComponentLogging.with_min_level"},{"category":"section","location":"#ComponentLogging","page":"Home","text":"ComponentLogging.jl is a lightweight, high-performance logging layer for Julia built around module-scoped component logging and hierarchical log-level control. A ComponentLogger applies hierarchical rules to groups such as :solver or (:solver, :iteration) and delegates accepted records to any AbstractLogger sink.\n\nThe package is designed for software where logging policy naturally belongs to a module or component and where filtered logging may sit on extremely hot paths.","title":"ComponentLogging"},{"category":"section","location":"#Logger-ownership:-component-scope-vs-task-scope","page":"Home","text":"Julia's standard Logging system dynamically selects the active logger from the current task, with a global logger as fallback. This makes the logger part of the current execution context: different tasks can carry different loggers, and application code can control logging for an entire dynamic call tree with with_logger.\n\nComponentLogging deliberately chooses a different default ownership model. A bound ComponentLogger represents shared logging configuration for a module or software component rather than execution-local state attached to the current task.\n\nFor example, a solver module can define one hierarchy:\n\n:solver\n(:solver, :iteration)\n(:solver, :linear_system)\n\nand all code using that module-level logger observes the same component configuration, independent of which task or thread happens to execute the code.\n\nThis is a deliberate trade-off, not a replacement for Julia's logging model. Julia's own documentation explicitly contrasts its dynamically scoped logger design with frameworks where loggers are lexically scoped or explicitly provided by module authors. ComponentLogging intentionally occupies that other point in the design space.","title":"Logger ownership: component scope vs task scope"},{"category":"section","location":"#Design-philosophy","page":"Home","text":"ComponentLogging is designed around one central constraint:\n\nFiltered logging should be as close to free as possible.\n\nIn performance-sensitive numerical and systems code, disabled log calls may execute millions of times. When the useful work of a filtered call is only a few nanoseconds, a task-local lookup, reader-side lock, allocation, or other per-call synchronization can easily dominate the cost.\n\nComponentLogging therefore keeps the normal read path deliberately small:\n\nmodule/component configuration is shared rather than discovered from task-local state on every call;\nthe explicit clog, clogf, and clogenabled APIs bypass task-local logger lookup entirely;\nnormal ComponentLogger reads do not acquire a configuration lock;\ncomparatively rare configuration changes pay the synchronization cost instead.\n\nThis makes the explicit function API particularly suitable for hot loops and performance-sensitive library internals. It also keeps logging behavior explicit when a logger is passed through a call chain.","title":"Design philosophy"},{"category":"section","location":"#Concurrency-model","page":"Home","text":"ComponentLogger is designed for safe concurrent use across tasks and threads. Thread-safe configuration updates were introduced in v0.2.0, and since v0.3.0 the implementation has used copy-on-write snapshots with atomic publication, keeping normal logging reads lock-free while preserving safe concurrent configuration updates. Module logger bindings use the same concurrency model.\n\nwith_min_level temporarily changes the minimum level globally for the target ComponentLogger, so all tasks and threads using that logger observe the temporary setting until it is restored.\n\nComponentLogger only routes and filters messages. Accepted records are delegated to the configured AbstractLogger sink, so thread safety of the actual message output is the responsibility of that sink.","title":"Concurrency model"},{"category":"section","location":"#Two-complementary-usage-modes","page":"Home","text":"Module-scoped convenience. Bind a logger to a module and use the macro or forwarding APIs when one shared logging policy should apply throughout that component.\n\nExplicit logger passing. When a particular task, request, solver instance, or other execution context needs its own logging policy, create a separate ComponentLogger and pass it explicitly:\n\nlogger_a = ComponentLogger(...)\nlogger_b = ComponentLogger(...)\n\nThreads.@spawn solve(problem_a, logger_a)\nThreads.@spawn solve(problem_b, logger_b)\n\nfunction solve(problem, logger)\n    clog(logger, :iteration, 0, \"starting iteration\")\nend\n\nThis provides task-specific logging without making every log call query task-local state. It is also the lowest-overhead path: the module registry is bypassed, and Julia can preserve the concrete ComponentLogger{L} type throughout the call chain.","title":"Two complementary usage modes"},{"category":"section","location":"#Beyond-logging","page":"Home","text":"The same hierarchy can also act as a lightweight runtime control plane. clogenabled can guard arbitrary work, while Boolean set_log_level! / set_log_level calls turn hierarchical groups into runtime switches without threading configuration flags through every function in the call stack.\n\nThis can be used for diagnostics, tracing, optional computations, algorithmic branches, caching strategies, instrumentation, or any other behavior placed behind the hierarchy.\n\nSee Hierarchical Runtime Control for the full pattern and examples.","title":"Beyond logging"},{"category":"section","location":"#Choosing-an-API","page":"Home","text":"API style Best suited for Logger lookup\nclog, clogf, clogenabled with an explicit logger Hot paths, libraries, execution-specific loggers None\n@forward_logger generated wrappers Module-local convenience with a known logger Resolved from the forwarded logger expression\n@clog, @cinfo, @clogenabled, ... Convenient module-bound logging with caller metadata Module registry\n\nThe Function API page covers the explicit and forwarded function interfaces. The Macros API page covers module-bound logging through the registry.","title":"Choosing an API"},{"category":"section","location":"#Performance-tracking","page":"Home","text":"ComponentLogging is continuously benchmarked over time. Current and historical performance results are available on the benchmark dashboard.","title":"Performance tracking"},{"category":"section","location":"#API-index","page":"Home","text":"Modules = [ComponentLogging]\nOrder   = [:type, :function, :macro]","title":"API index"}]
}
